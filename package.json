{
  "name": "emappa",
  "private": true,
  "version": "0.1.0",
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "mobile",
    "website",
    "cockpit",
    "packages/*"
  ],
  "scripts": {
    "postinstall": "patch-package",
    "dev": "turbo dev",
    "build": "turbo build",
    "build:website": "npm run build --workspace @emappa/website",
    "build:cockpit": "npm run build --workspace @emappa/cockpit",
    "dev:website": "npm run dev --workspace @emappa/website",
    "dev:cockpit": "npm run dev --workspace @emappa/cockpit",
    "dev:mobile": "npm run dev --workspace @emappa/mobile",
    "dev:backend": "cd backend && .venv/bin/uvicorn app.main:app --reload",
    "test:backend": "cd backend && .venv/bin/python -m pytest tests -q",
    "migrate": "cd backend && .venv/bin/alembic upgrade head",
    "migrate:revise": "cd backend && .venv/bin/alembic revision -m \"$npm_config_msg\"",
    "seed": "cd backend && EMAPPA_DEV_SEED=true .venv/bin/python -m scripts.seed",
    "grant:admin": "cd backend && .venv/bin/python -m scripts.grant_admin",
    "audit:shared": "npm run audit --workspace @emappa/shared",
    "audit:missing": "node scripts/audit-missing.mjs",
    "test": "npm run test --workspace @emappa/shared",
    "test:shared": "npm run test --workspace @emappa/shared",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint",
    "ci": "npm run audit:shared && npm run test:shared && npm run typecheck && npm run lint && npm run build && npm run test:backend"
  },
  "overrides": {
    "postcss": "8.5.12",
    "uuid": "14.0.0"
  },
  "devDependencies": {
    "patch-package": "^8.0.1",
    "turbo": "2.9.6",
    "typescript": "5.9.3"
  }
}
